## Codex CLI との協働

ユーザーが「Codexに依頼して」「Codexでレビューして」等の指示をした場合は、`/codex` スキルを使用してください。

### 進め方

1. 依頼の目的と期待する成果を明確にする
2. `/codex` で Codex CLI に委譲（読み取り専用サンドボックスで実行）
3. Codex の出力をそのまま採用せず、妥当性を判断したうえで統合し、ユーザーに分かりやすく提示する

最終的な判断と統合は Claude Code が担当します。
